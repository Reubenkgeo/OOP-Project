README
This is a piece of code written in C++ which takes a Boolean expression as input, then proceeds to explain the operators which are used, then generates a formatted truth table which shows every possible combination of inputs and their results.
